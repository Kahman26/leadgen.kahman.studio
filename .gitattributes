# Скрипты и конфиги всегда с LF: файл с CRLF на Linux падает
# с «bad interpreter: No such file or directory».
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
*.yml       text eol=lf
*.py        text eol=lf
# Батник остаётся в windows-виде
*.bat       text eol=crlf
